// Source file: wintools.c
int AstralStr( int idString, LPSTR FAR * lppString );
BOOL StringsEqualID( LPSTR lpString1, ITEMID idString2 );
DWORD GetWinXY(HWND hWnd);
int ColorText( HDC hDC, int x, int y, LPSTR szStr, int length, COLOR Color );
void CenterPopup( HWND hWnd );
void GetScreenCenter(HWND hWnd, LPPOINT ppt, int dx, int dy);
void OffsetWindow( HWND hWnd, int dx, int dy, BOOL bRedraw );
void RightPopup( HWND hWnd );
void LeftPopup( HWND hWnd );
void SavePopup( HWND hWnd, LPSTR lpKeyName );
BOOL StyleOn( HWND hWnd, DWORD dwFlag );
BOOL IsWordBitSet( HWND hWnd, int nOffset, WORD wBit );
void SetWordBit( HWND hWnd, int nOffset, WORD wBit, BOOL on );
void SetLongBit( HWND hWnd, int nOffset, LONG lBit, BOOL on );
void DisableSavebits( HWND hWnd );
void EnableSavebits( HWND hWnd );
void SetClientRect( HWND hWnd, LPRECT lpRect );
void GetDrawClientRect( HWND hWnd, LPRECT lpRect );
void GetRealClientRect( HWND hWnd, LPRECT lpRect );
void GetScrollBarSize( LPINT lpCx, LPINT lpCy );
void SetFullClientRect( HWND hWnd, BOOL fFull );
VOID AstralMenuCheck( int menuID, int * oldmenuID );
VOID AstralSetDlgItemFixed( HWND hDlg, int idDlg, LFIXED Fixed, BOOL bNoConvert );
int SetUnitResolution( int res );
static void DoubleToString(double d, LPSTR lpString, int iPrecision);
LPSTR Unit2String( LFIXED Fixed, LPSTR lpFixed );
static BOOL isValidFixed(LPSTR lpFixed);
LFIXED AstralGetDlgItemFixed( HWND hDlg, int idDlg, BOOL FAR * lpTranslated, BOOL bNoConvert );
void DrawGuideLine(HDC hDC, int x1, int y1, int x2, int y2, BOOL on);
void Dline( HDC hDC, int x1, int y1, int x2, int y2, BOOL flag );
void DrawLine( HDC hDC, int x1, int y1, int x2, int y2, HPEN hPen );
void DrawLineEx( HDC hDC, int x1, int y1, int x2, int y2, HPEN hPen, BOOL bXor );
void Drect( HDC hDC, LPRECT lpRect, BOOL flag );
void DrawRect( HDC hDC, LPRECT lpRect, HPEN hPen );
void Dbox( HDC hDC, LPRECT lpRect, BOOL flag );
void Dellipse( HDC hDC, LPRECT lpRect, BOOL flag );
VOID SetKeyState( int nVirtKey, BOOL fDown, BOOL fToggle );
void ShowControl( HWND hDlg, int idControl1, int idControl2, BOOL bShow, BOOL bDefer );
void ShowDlgItems( HWND hDlg, int idControl1, int idControl2, BOOL bShow, BOOL bDefer );
void ShowDlgItem( HWND hDlg, ITEMID idControl, BOOL bShow );
void ControlEnableGroup( HWND hDlg, int idControl1, int idControl2, BOOL bEnable );
HBRUSH SetControlColors(HDC hDC, HWND hDlg, HWND hControl, WORD wType);
void HilightRect( HDC hDC, LPRECT lpRect, HBRUSH hBrush );
void HilightFrame( HDC hDC, LPRECT lpRect, HBRUSH hBrush, int iWidth, BOOL bSetColors );
void GrayWindow( HDC hDC, HWND hWindow, DWORD dwStyle );
void GrayWholeWindow( HDC hDC, HWND hWindow, DWORD dwStyle );
void MgxGrayArea( HDC hDC, int x, int y, int iWidth, int iHeight);
HWND CopyWindow( HWND hWnd );
BOOL EnableOverlappedWindow( HWND hWnd, BOOL bEnable );
HINSTANCE AstralLoadLibrary(LPSTR lpLibraryName);
void Delay( long lTicks );
BOOL DelayIfMouseDown( long lTicks );
void SendMessageToChildren(HWND hParent, WORD msg, WORD wParam, long lParam);
void DrawBevel( HDC hDC, LPRECT lpRect, BOOL bBevelin, int iWidth );
void DrawCorners( HDC hDC, LPRECT lpRect, BOOL bBevelin );
void SetClipDC( HDC hDC, LPRECT lpRect );
BOOL SelectClipRect(HDC hDC, LPRECT lpNewRect, LPRECT lpOldRect );
void DelUnusedSysMenuItems(HWND hWnd, BOOL fGrayItems, BOOL fSize);
void SetDlgItemIntExt( HWND hDlg, ITEMID id, int iValue, LPSTR lpString );
void SetDlgItemTextNow(HWND hDlg, int idCtl, LPSTR lpText);
void SetDlgItemTextMultiple(HWND hDlg, int idFirstCtl, int idLastCtl, LPSTR lpText);
void SetDlgItemIntNow(HWND hDlg, int idCtl, UINT uValue, BOOL fSigned);
BOOL AutoScroll( HWND hWindow, int x, int y);
void ShowExtra(HWND hParent, int idFirst, int idLast, int idShow);
HWND GetSafeChildWindow(HWND hParent, int idControl);
BOOL TransBlt( HDC hDstDC, int x, int y, int dx, int dy, HDC hSrcDC, int x0, int y0 );
LPSTR GetString (UINT idString,LPSTR lpBuffer);
BOOL SetControlFont (HWND hWnd, int iCtrlId);

